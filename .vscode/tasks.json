{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Docker image",
            "type": "shell",
            "command": "docker build -t arena ."
        },
        {
            "label": "Build Docker image (amd64-tiny)",
            "type": "shell",
            "command": "docker build -f Dockerfile.amd64-tiny -t arena ."
        }
    ]
}